(set-option :produce-assignments true)
(set-option :produce-models true)
(set-option :mbqi true)
(set-logic AUFLIA)
(reset)
(set-logic AUFLIA)
(declare-fun f (Int Int) Int)
(declare-const a Int)
(declare-const b Int)
(declare-sort A)
(assert (forall ((x Int)) (>= (f x x) (+ x a))))

(assert (< (f a b) a))
(assert (> a 0))
(check-sat)
(get-model)

(echo "evaluating (f (+ a 10) 20)...")
(eval (f (+ a 10) 20))
; uninterpreted functions
(declare-fun x () Int)
(declare-fun y () Int)
(declare-fun z () Int)
(define-fun eq ((x Bool) (y Bool)) Bool (ite (= x y) true false))
(check-sat)
(eval (eq true false))